const f = require('./node_modules/funcoes')
const lista = require('./node_modules/lista')
const mat = require('./node_modules/matematica')

f.mostrar(f.nome)
lista.forEach(item=>console.log(item))
mat.som(10,10)
